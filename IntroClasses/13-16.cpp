//Uses the die class to simulate the rolling of dice
/*
#include <iostream>
#include "die.h"
using namespace std;

int main()
{
	const int DIE1 = 6, DIE2 = 12, MAX_ROLL = 5;

	Die die1(DIE1); Die die2(DIE2);	//instances of the die class

	cout << "This will simulate the rolling of a " << die1.getSides() << " die.\n"
		<< "And a " << die2.getSides() << " die.\n";

	//get the initial values
	cout << "The initial values are: " << die1.getValue() << " and " << die2.getValue() << endl;

	cout << "Rolling the dice " << MAX_ROLL << " times.\n";
	//roll the dice five times
	for (int i = 0; i < MAX_ROLL; i++)
	{
		die1.roll();
		die2.roll();
		
		cout << die1.getValue() << " " << die2.getValue() << endl;
	}

	system("pause");
	return 0;
}
*/