/*

#include <iostream>
#include <memory>
#include "rectangle2.h"
using namespace std;

int main()
{
	unique_ptr<Rectangle2>box(new Rectangle2);

	cout << "Here is the box's data atributes:\n";
	cout << "Length: " << box->getLength() << endl;
	cout << "Width: " << box->getWidth() << endl;
	cout << "Area: " << box->getArea() << endl;

	return 0;
}
*/
//the constructors we've looked at are called default constructors. They have no arguments.
//However, just like many other functions-constructors can have arguments in them. 